export { validate, ValidationError, type Validator } from "@/shared/validation/types";
export {
  array,
  boolean,
  number,
  object,
  optionalValue,
  record,
  string,
  unknownLike,
} from "@/shared/validation/validator";
